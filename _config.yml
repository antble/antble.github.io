name: AVC
markdown: kramdown
kramdown:
  input: GFM
  math_engine: mathjax
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    disable : true

highlighter: rouge
  
rouge:
  style: base16.monokai

plugins:
  - jekyll-gist
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-feed

theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-scholar
